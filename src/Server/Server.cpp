#include <Server/Server.h>
namespace Server
{
    /**
      * Constructeur
      */
    Server::Server(qint32 port)
    {
        init(port);
    }
    /**
      * Initialise le serveur
      */
    void Server::init(qint32 port)
    {
        //TODO
    }
}
